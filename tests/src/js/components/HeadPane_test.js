// __test__/HeadPane-test.js
import expect from 'expect.js';

import React from 'react';
import { renderShallow } from '../../../test_helper';
/*import HeadPane from '../../../../src/js/components/HeadPane';

describe('HeadPane', () => {
	let headerPane;

	beforeEach(()=>{
		headerPane = renderShallow(<HeadPane twitter="@betobaz" />);
	});

	it('Debe de decir hola mundo', () => {

		console.log(headerPane.props.children);
		debugger;
		expect(headerPane.props.children).to.be("Hola @betobaz");

	});
});*/